import 'package:flutter/material.dart';

class PointHistoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Point History'),
      ),
      body: Center(
        child: Text('Point History Page - Under Construction'),
      ),
    );
  }
}